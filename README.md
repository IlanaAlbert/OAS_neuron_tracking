# OAS_neuron_tracking
manually label neurons and get intensity from each

To create a conda environment with the correct packages, install anaconda and do the following:

conda create -n worm_tracker python=3.10
conda activate worm_tracker

conda install numpy scipy matplotlib scikit-image h5py opencv pillow openpyxl
